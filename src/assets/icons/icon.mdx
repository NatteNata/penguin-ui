import {IconGallery, IconItem, Meta} from "@storybook/blocks"
import * as Icons from "./components"

<Meta title="Design system/Icons"/>
## Compilation Command to convert svg-icons to separate components

npx @svgr/cli --out-dir src/assets/icons/components --jsx-runtime automatic --typescript --ref --memo --no-dimensions --icon -- src/assets/icons/icons-origin

## Usage

All the icons in the design system are available as React components.


## Gallery

<br/>

<IconGallery>
    <div className={'grid grid-cols-3 gap-4'}>
        {Object.entries(Icons)
            .filter(([name]) => name !== 'ChevronDown')
            .map(([name, Icon]) => (
                <IconItem key={name} name={name}>
                    <Icon/>
                </IconItem>
            ))}
    </div>
</IconGallery>
